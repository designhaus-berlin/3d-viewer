---
phase: 02-dokumentation-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "docs/index.html lädt DB UX von CDN (cdn.jsdelivr.net/npm/@db-ux) — kein self-hosted vendor/db-ux/"
    - "AE86-Modell erscheint im Viewer-iframe auf der Demo-Seite"
    - "AE86-Modell lädt über relativen Pfad ?model=../assets/ae86.glb (docs/ → Repo-Root assets/)"
    - "db-header zeigt Projektname und GitHub-Link"
    - "db-tabs unter dem Viewer zeigen mindestens 2 Tabs"
    - "Jeder Tab-Panel enthält ein Accordion mit Kamera-Preset-Gruppe und kurzem Beschreibungstext"
    - "Klick auf Kamera-Preset-Button sendet animateCamera/focus Command an den Viewer"
    - "docs/index.html ist lokal erreichbar: http://localhost/3d-viewer/docs/index.html"
    - "Nach GitHub Pages Deployment erreichbar: designhaus-berlin.github.io/3d-viewer/docs/index.html (manueller Checkpoint)"
  artifacts:
    - path: "docs/index.html"
      provides: "Demo-Seite mit Hero-Layout (Viewer oben, Tabs + Accordion darunter), DB UX Komponenten"
  key_links:
    - from: "docs/index.html"
      to: "../viewer/index.html"
      via: "iframe src (relativ)"
      pattern: "src=\"../viewer/index.html"
    - from: "docs/index.html"
      to: "cdn.jsdelivr.net/npm/@db-ux/core-foundations@4.5.0"
      via: "<link rel=stylesheet href=https://cdn.jsdelivr.net/.../rollup.css>"
      pattern: "cdn.jsdelivr.net/npm/@db-ux/core-foundations"
    - from: "docs/index.html"
      to: "cdn.jsdelivr.net/npm/@db-ux/wc-core-components@4.5.0"
      via: "<script type=module src=https://cdn.jsdelivr.net/.../db-ux.js>"
      pattern: "cdn.jsdelivr.net/npm/@db-ux/wc-core-components"
    - from: "db-button onclick"
      to: "iframe.contentWindow.postMessage"
      via: "send(type, payload) Helper"
      pattern: "send\\('animateCamera'|send\\('focus'"
---

<objective>
Die GitHub Pages Demo-Seite docs/index.html bauen: Hero-Layout mit Viewer-iframe oben und
Kamera-Preset-Tabs mit Accordion darunter. DB UX v4.5.0 wird per jsDelivr CDN eingebunden
(kein Self-Hosting — DB ist Kunde, CDN-Abhängigkeit akzeptiert per D-10).

Purpose: DOC-02 erfüllen — Live-Demo auf designhaus-berlin.github.io/3d-viewer/docs/index.html.

Output:
- docs/index.html — vollständige Demo-Seite mit DB UX Komponenten (CDN) und AE86-Viewer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dokumentation-demo/02-CONTEXT.md
@.planning/phases/02-dokumentation-demo/02-RESEARCH.md

<interfaces>
<!-- Viewer iframe — URL-Parameter aus viewer/index.html Import Map -->
<!-- GitHub Pages Repo-Root-Deployment (D-18): GitHub Pages Source = master / (root) -->
<!-- docs/index.html liegt dann bei /3d-viewer/docs/index.html -->
<!-- Relative Pfade aus docs/ heraus: -->
<!--   viewer: ../viewer/index.html  (docs/ → root → viewer/) -->
<!--   assets: ../assets/ae86.glb   (docs/ → root → assets/) -->
<!--   vendor:  ../vendor/           (docs/ → root → vendor/) -->

<!-- postMessage send-Helper (aus host-example/index.html, bewährt): -->
```js
const viewer = document.getElementById('viewer');
function send(type, payload) {
  viewer.contentWindow.postMessage({ type, payload }, '*');
}
```

<!-- Viewer iframe src (per D-19, D-20, D-02): -->
<iframe
  id="viewer"
  src="../viewer/index.html?model=../assets/ae86.glb&theme=light"
  sandbox="allow-scripts allow-same-origin allow-pointer-lock allow-popups"
  allow="xr-spatial-tracking; fullscreen"
  referrerpolicy="no-referrer">
</iframe>

<!-- DB UX HTML-Tags aus der Verwendung in host-example/ + RESEARCH.md: -->
<!-- CSS: <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@db-ux/core-foundations@4.5.0/build/styles/rollup.css"> -->
<!-- JS:  <script type="module" src="https://cdn.jsdelivr.net/npm/@db-ux/wc-core-components@4.5.0/dist/esm/db-ux.js"></script> -->
<!-- data-color-scheme="light" auf <html> für DB UX Light Mode -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDN-Links für DB UX v4.5.0 in docs/index.html Head einbinden</name>
  <files>docs/index.html</files>
  <action>
    Stelle sicher, dass docs/index.html im `<head>` folgende zwei Zeilen enthält
    (per D-10: DB UX von jsDelivr CDN, kein Self-Hosting):

    ```html
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@db-ux/core-foundations@4.5.0/build/styles/rollup.css">
    <script type="module" src="https://cdn.jsdelivr.net/npm/@db-ux/wc-core-components@4.5.0/dist/esm/db-ux.js"></script>
    ```

    Falls docs/index.html noch nicht existiert: Task 2 erstellt sie vollständig —
    diese beiden Zeilen müssen dort bereits enthalten sein.

    Falls docs/index.html aus einem früheren Stand noch `vendor/db-ux/`-Pfade enthält:
    Diese durch die CDN-URLs oben ersetzen.

    KRITISCH: `rollup.css` verwenden (nicht `relative.css`). `rollup.css` ist die
    CDN-taugliche Variante — sie hat absolute Asset-Pfade, die der Browser direkt
    von CDN laden kann.

    Kein vendor/db-ux/-Verzeichnis erstellen. Kein curl. Nur die zwei HTML-Zeilen.
  </action>
  <verify>
    <automated>grep -c "cdn.jsdelivr.net/npm/@db-ux" docs/index.html 2>/dev/null || echo "0"</automated>
  </verify>
  <done>
    docs/index.html enthält im Head mindestens 2 Treffer für cdn.jsdelivr.net/npm/@db-ux
    (eine für core-foundations CSS, eine für wc-core-components JS).
    Kein vendor/db-ux/-Pfad in docs/index.html.
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/index.html — Demo-Seite mit Hero + Tabs + Accordion erstellen</name>
  <files>docs/index.html</files>
  <action>
    Erstelle docs/index.html als vollständige Showcase-Demo (per D-01 bis D-11, D-18 bis D-20).
    Die Seite ist auf Deutsch (D-11). DB UX Komponenten kommen vom jsDelivr CDN (D-10).

    **Layout (D-03):** Einteiliges Scroll-Layout:
    1. `<db-header>` — oben, volle Breite
    2. Hero-Bereich: Viewer-iframe nimmt 60–70 vh ein (groß, im Vordergrund, D-01)
    3. Tab-Bereich: `<db-tabs>` direkt darunter (D-07)
    4. Tab-Inhalt: pro Tab ein `<db-accordion>` mit Kamera-Preset-Gruppe + kurzem Beschreibungstext (D-08, D-09)

    **Was NICHT rein darf (D-05, Deferred):**
    - Keine Sichtbarkeit-Toggles
    - Keine Live-API-Console
    - Keine Code-Beispiele auf der Seite (D-01)

    **GitHub Pages Deployment (D-18):**
    GitHub Pages muss auf **Repo-Root** konfiguriert sein:
    Settings → Pages → Source: **master** Branch, Ordner **(root)** (NICHT /docs).
    Mit Repo-Root-Serving gilt:
    - Demo-URL: `designhaus-berlin.github.io/3d-viewer/docs/index.html`
    - iframe src `../viewer/index.html` löst auf `/3d-viewer/viewer/index.html` auf ✓
    - model-Param `../assets/ae86.glb` löst auf `/3d-viewer/assets/ae86.glb` auf ✓
    Lokal entspricht der Pfad exakt: `http://localhost/3d-viewer/docs/index.html` — kein Unterschied.

    **AE86-Asset-Pfad (D-20):**
    Der iframe src nutzt `?model=../assets/ae86.glb`. Von docs/ traversiert `../` zum Repo-Root,
    dort liegt `assets/ae86.glb`. Kein File-Copy nötig.

    **HTML-Struktur:**

    ```html
    <!doctype html>
    <html lang="de" data-color-scheme="light">
    <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>3D Viewer – Live Demo</title>
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@db-ux/core-foundations@4.5.0/build/styles/rollup.css">
      <script type="module" src="https://cdn.jsdelivr.net/npm/@db-ux/wc-core-components@4.5.0/dist/esm/db-ux.js"></script>
      <style>
        /* Nur Layout — Design-Tokens kommen aus DB UX */
        *, *::before, *::after { box-sizing: border-box; }
        body { margin: 0; background: var(--db-color-background); font-family: var(--db-font-family-body, system-ui, sans-serif); }
        .hero { width: 100%; aspect-ratio: 16/9; max-height: 70vh; background: #0f1115; }
        .hero iframe { width: 100%; height: 100%; border: 0; display: block; }
        .tabs-wrap { padding: var(--db-spacing-fixed-md, 16px); }
        .preset-grid { display: flex; flex-wrap: wrap; gap: var(--db-spacing-fixed-sm, 8px); padding: var(--db-spacing-fixed-sm, 8px) 0; }
      </style>
    </head>
    <body>

    <db-header>
      <span slot="logo">3D Viewer</span>
      <a slot="action" href="https://github.com/designhaus-berlin/3d-viewer" target="_blank" rel="noopener">GitHub</a>
    </db-header>

    <main>
      <section class="hero">
        <iframe
          id="viewer"
          src="../viewer/index.html?model=../assets/ae86.glb&theme=light"
          sandbox="allow-scripts allow-same-origin allow-pointer-lock allow-popups"
          allow="xr-spatial-tracking; fullscreen"
          referrerpolicy="no-referrer"
          title="Toyota AE86 3D-Viewer">
        </iframe>
      </section>

      <section class="tabs-wrap">
        <db-tabs>
          <db-tab-list>
            <db-tab>Außenansichten</db-tab>
            <db-tab>Räder &amp; Details</db-tab>
          </db-tab-list>

          <db-tab-panel>
            <db-accordion>
              <db-accordion-item>
                <span slot="title">Kamera-Presets — Außenansichten</span>
                <p>Wähle eine Perspektive um den Toyota AE86 von außen zu betrachten.</p>
                <div class="preset-grid">
                  <db-button onclick="send('animateCamera', {position:{x:0,y:2.5,z:8}, target:{x:0,y:1.2,z:0}, durationMs:900})">Frontansicht</db-button>
                  <db-button onclick="send('animateCamera', {position:{x:8,y:2.5,z:0}, target:{x:0,y:1.2,z:0}, durationMs:900})">Seitenansicht rechts</db-button>
                  <db-button onclick="send('animateCamera', {position:{x:-8,y:2.5,z:0}, target:{x:0,y:1.2,z:0}, durationMs:900})">Seitenansicht links</db-button>
                  <db-button onclick="send('animateCamera', {position:{x:0,y:2.5,z:-8}, target:{x:0,y:1.2,z:0}, durationMs:900})">Heckansicht</db-button>
                  <db-button onclick="send('animateCamera', {position:{x:5,y:6,z:5}, target:{x:0,y:1.2,z:0}, durationMs:900})">Vogelperspektive</db-button>
                </div>
              </db-accordion-item>
            </db-accordion>
          </db-tab-panel>

          <db-tab-panel>
            <db-accordion>
              <db-accordion-item>
                <span slot="title">Kamera-Presets — Räder &amp; Details</span>
                <p>Zoome auf einzelne Räder oder das Cockpit für Detailansichten.</p>
                <div class="preset-grid">
                  <db-button onclick="send('focus', {selector:'wheel1', padding:1.5, durationMs:800})">Vorderrad links</db-button>
                  <db-button onclick="send('focus', {selector:'wheel2', padding:1.5, durationMs:800})">Vorderrad rechts</db-button>
                  <db-button onclick="send('focus', {selector:'wheel3', padding:1.5, durationMs:800})">Hinterrad links</db-button>
                  <db-button onclick="send('focus', {selector:'wheel4', padding:1.5, durationMs:800})">Hinterrad rechts</db-button>
                  <db-button onclick="send('animateCamera', {position:{x:2.5,y:3.5,z:6}, target:{x:0,y:2.8,z:0}, durationMs:900})">Cockpit</db-button>
                </div>
              </db-accordion-item>
            </db-accordion>
          </db-tab-panel>
        </db-tabs>
      </section>
    </main>

    <script>
      const viewer = document.getElementById('viewer');
      function send(type, payload) {
        viewer.contentWindow.postMessage({ type, payload }, '*');
      }

      // modelReady empfangen — optional: Lade-Indikator ausblenden
      window.addEventListener('message', function(e) {
        if (!e.data || e.data.source !== 'viewer') return;
        if (e.data.type === 'error') {
          console.warn('[Demo] Viewer-Fehler:', e.data.payload);
        }
      });
    </script>

    </body>
    </html>
    ```

    **Hinweise zur Implementierung:**

    Slot-Namen für db-header: Aus den Stencil-Patterns abgeleitet. Falls `slot="logo"` oder
    `slot="action"` nicht funktionieren, beim Test in der Browser-Konsole prüfen und auf
    `slot="brand"` oder plain-Content ohne Slot ausweichen — die RESEARCH.md markiert die
    Slot-Namen als [ASSUMED].

    db-accordion / db-accordion-item: Falls die Komponenten andere Slot-API verwenden als gezeigt
    (z.B. `summary` statt `title`), custom-elements.json aus vendor/db-ux/wc/esm/ lesen (falls
    vorhanden) oder zur Laufzeit in DevTools prüfen. Notfalls `<details>`/`<summary>` mit DB UX
    Klassen als CSS-only Fallback verwenden.

    db-tabs / db-tab-panel: Falls die Komponenten andere Slot-API verwenden als gezeigt,
    custom-elements.json aus vendor/db-ux/wc/esm/ lesen (falls vorhanden) oder
    zur Laufzeit in DevTools prüfen. Notfalls CSS-only Tabs mit DB UX Klassen bauen
    (db-tab-list → `<ul class="db-tab-list">`) — das ist ein valider Fallback.

    **Kamera-Koordinaten:** Die gezeigten Koordinaten sind vernünftige Startpunkte für ein
    Auto-Modell in normalisierten Blender-Einheiten (Modell bei Y=0, ca. 2m hoch).
    Falls das AE86-Modell deutlich anders skaliert ist, muss der Executor die Koordinaten
    per Debug-GUI (?gui=1) ablesen und anpassen. Das ist Claude's Discretion gemäß CONTEXT.md.

    **Lokal testen:** http://localhost/3d-viewer/docs/index.html — entspricht exakt dem
    GitHub Pages Pfad (Repo-Root-Serving, D-18).
  </action>
  <verify>
    <automated>
      curl -s http://localhost/3d-viewer/docs/index.html | grep -c "db-header" &amp;&amp; curl -s http://localhost/3d-viewer/docs/index.html | grep -c "cdn.jsdelivr.net/npm/@db-ux" &amp;&amp; curl -s http://localhost/3d-viewer/docs/index.html | grep -c "db-accordion" &amp;&amp; echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    docs/index.html existiert und enthält:
    - db-header mit GitHub-Link
    - iframe mit src="../viewer/index.html?model=../assets/ae86.glb"
    - db-tabs mit mindestens 2 Tab-Panels
    - db-accordion in jedem Tab-Panel mit Kamera-Preset-Buttons und Beschreibungstext
    - mindestens 5 db-button Kamera-Preset-Buttons gesamt
    - CDN-Referenzen auf cdn.jsdelivr.net/npm/@db-ux/ für CSS und JS vorhanden
    - KEINE Referenz auf vendor/db-ux/ (kein Self-Hosting)
    Lokal via http://localhost/3d-viewer/docs/index.html erreichbar.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    DB UX v4.5.0 per jsDelivr CDN eingebunden und Demo-Seite docs/index.html mit
    Hero-Viewer, Kamera-Preset-Tabs und Accordion je Tab.
  </what-built>
  <how-to-verify>
    1. Browser öffnen: http://localhost/3d-viewer/docs/index.html
    2. DevTools → Network → Requests: cdn.jsdelivr.net/npm/@db-ux/ Requests für CSS und JS
       sichtbar (erwartet — CDN ist per D-10 akzeptiert). Kein fonts.googleapis.com, kein unpkg.
    3. Seite laden abwarten — AE86-Modell muss im Viewer erscheinen (kein weißer Bildschirm)
    4. DB UX Header sichtbar (Projektname + GitHub-Link)
    5. Tabs unter dem Viewer sichtbar; Tab wechseln funktioniert
    6. Accordion in jedem Tab sichtbar und aufklappbar; Beschreibungstext erscheint
    7. Einen Kamera-Preset-Button klicken → Kamera-Animation im Viewer läuft
    8. Nach erfolgreichem lokalem Test: GitHub Pages aktivieren (Settings → Pages → Source:
       master / (root)) und designhaus-berlin.github.io/3d-viewer/docs/index.html aufrufen
    9. Falls DB UX Web Components nicht rendern (plain Text statt Buttons/Tabs/Accordion):
       DevTools Console auf Fehler prüfen und im Resume-Signal beschreiben
  </how-to-verify>
  <resume-signal>Type "approved" wenn alles funktioniert (lokal + GitHub Pages), oder beschreibe gefundene Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe → parent page | postMessage von viewer zu demo-Seite; gleicher Origin (github.io) |
| parent page → iframe | send() ruft postMessage mit `*` als targetOrigin auf |
| browser → cdn.jsdelivr.net | DB UX CSS + JS per CDN — akzeptiert per D-10 (DB ist Kunde) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | postMessage send() mit `'*'` targetOrigin | accept | Viewer-Demo ist öffentlich; kein sensitives Payload; `'*'` ist Standardpraxis für same-domain embedding. Kommentar im Code weist auf konkreten Origin für Produktion hin. |
| T-02-02 | Information Disclosure | CDN-Abhängigkeit jsDelivr für DB UX | accept | DB ist Kunde — CDN-Abhängigkeit per D-10 akzeptiert. Demo-Seite ist nicht für vertrauliche Daten. jsDelivr ist etablierter, stabiler CDN-Anbieter. |
| T-02-03 | Tampering | vendor/db-ux/ Dateien nachträglich verändert | accept | Kein Produktions-Backend; statische Files im Git-Repo. Integrität durch Git-History sichergestellt. |
| T-02-04 | Denial of Service | Iframe lädt Viewer mit großem Modell | accept | AE86 = 812 KB — unkritisch. Kein user-controlled model-Parameter auf der Demo-Seite. |
| T-02-05 | Elevation of Privilege | sandbox-Attribut am iframe | mitigate | iframe hat `sandbox="allow-scripts allow-same-origin allow-pointer-lock allow-popups"` — kein `allow-forms`, kein `allow-top-navigation`. Viewer kann nicht aus dem Sandbox-Kontext ausbrechen. |
</threat_model>

<verification>
Nach Abschluss beider Auto-Tasks und vor dem Checkpoint gilt:

1. `curl -s http://localhost/3d-viewer/docs/index.html | grep "cdn.jsdelivr.net/npm/@db-ux"` → mindestens 2 Treffer (CSS + JS)
2. `curl -s http://localhost/3d-viewer/docs/index.html | grep "vendor/db-ux"` → leere Ausgabe (kein Self-Hosted-Pfad)
4. `curl -s http://localhost/3d-viewer/docs/index.html | grep "db-header"` → mindestens 1 Treffer
5. `curl -s http://localhost/3d-viewer/docs/index.html | grep "viewer/index.html"` → mindestens 1 Treffer
6. `curl -s http://localhost/3d-viewer/docs/index.html | grep "db-accordion"` → mindestens 1 Treffer
</verification>

<success_criteria>
- docs/index.html lädt DB UX CSS und JS von cdn.jsdelivr.net/npm/@db-ux/ (CDN per D-10 akzeptiert)
- docs/index.html rendert im Browser mit DB UX Custom Elements korrekt
- AE86-Modell lädt im eingebetteten Viewer-iframe über relativen Pfad ../assets/ae86.glb
- Jeder Tab-Panel enthält ein aufklappbares Accordion mit Kamera-Presets und Beschreibungstext
- Kamera-Preset-Buttons lösen postMessage-Commands aus (sichtbare Kamera-Animation)
- DB UX Custom Elements (db-header, db-tabs, db-accordion, db-button) sind sichtbar und gestyled
- GitHub Pages Konfiguration: master / (root) — Demo live unter designhaus-berlin.github.io/3d-viewer/docs/index.html
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02-dokumentation-demo/02-01-SUMMARY.md` erstellen mit:
- Tatsächliche vendor/db-ux/ Struktur (welche Dateien wurden heruntergeladen)
- GitHub Pages Konfigurationshinweis: Settings → Pages → Source: master / (root) — NICHT /docs
- Welche DB UX Slot-Namen tatsächlich funktioniert haben (db-header, db-tabs, db-accordion)
- Finale Kamera-Koordinaten der Presets (nach eventuellem Anpassen)
- Etwaige Abweichungen vom Plan (z.B. CSS-only Accordion als Fallback)
</output>
